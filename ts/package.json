{
  "devDependencies": {
    "@types/jest": "^29.5.11",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.1"
  },
  "scripts": {
    "test": "jest",
    "test:watch": "nodemon --exec npm test --watch **/*.ts"
  }
}